{
	"name": "react-bnb",
	"version": "0.1.0",
	"scripts": {
		"install": "cd common && npm install && cd ../client && npm install && cd ../server && npm install",
		"build": "cd common && npm run build && cd ../client && npm run build && cd ../server && npm run build",
		"start": "cd server && npm start",
		"start-dev": "concurrently \"cd server && npm run start-dev\" \"cd client && npm start\""
	},
	"dependencies": {},
	"devDependencies": {
		"concurrently": "^5.3.0"
	}
}
